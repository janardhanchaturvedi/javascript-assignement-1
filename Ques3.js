// Write a function that takes two numbers as argument and return their sum. Declare a variable inside the function using the var keywords and logs its values to the console before it is assigned a value to demostrate variable hosting
function sumNum(num1 , num2 ){
    console.log(add);
    var add;
    add = num1 + num2;
    return add;
}


console.log(sumNum(5, 10));